Implement the Quick Start UI changes in the existing vanilla JS/CSS structure.

1. Add a nested credential tab state (`credentialsSubTab`, default `password`) alongside the existing Security sub-tabs.
2. Keep the current outer Security tabs: School Information, Admin Credentials, and Teachers.
3. Replace the two-column Admin Credentials card layout with a compact nested tab bar containing:
   - Change Admin Password
   - Recovery Code
4. Render each credential form in its own tab panel, preserve the existing form IDs, validation, API calls, and success behavior, and update active tab/ARIA/tabindex state through a new `switchCredentialsSubTab` handler.
5. Replace raw emoji-only button icons in the Quick Start modal with explicit inline SVG icon wrappers for the action buttons (Add, AI generation, backup loading, school save, password update, recovery code, teacher creation). Keep the existing emoji for step/card headings where appropriate.
6. Add scoped Quick Start CSS so all icon wrappers have fixed dimensions, explicit currentColor/contrast, consistent spacing, and clear active/inactive tab states. Update the credential area to use a single-column tabbed layout and remove the old two-column grid dependency.
7. Add responsive handling so the nested credential tabs remain usable on small screens.
8. Verify the result with the existing build/test commands and inspect the final diff without modifying the unrelated `prisma/dev.db` change.